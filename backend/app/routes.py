"""
routes.py – API route definitions.

TODO: Add authentication routes (register, login, logout).
TODO: Add creature routes (list spawns, catch creature).
TODO: Add user collection routes (view caught creatures).
TODO: Add admin routes (force spawn, manage creature library).
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Register route modules, e.g.:
# from app.routers import auth, creatures, collection, admin
# router.include_router(auth.router, prefix="/auth", tags=["auth"])
# router.include_router(creatures.router, prefix="/creatures", tags=["creatures"])
